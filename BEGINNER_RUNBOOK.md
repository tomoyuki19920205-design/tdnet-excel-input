# 📘 TDnet決算データ 運用ガイド（初心者向け）

このガイドだけで、決算データの自動更新ができます。
ターミナル（黒い画面）の操作は不要です。

---

## ⭐ まず最初にやること

### `run_update_all.bat` をダブルクリック

これだけで以下が自動実行されます:

1. TDnetから最新の決算短信を取得
2. データベースを更新
3. `data.xlsx` を生成して OneDrive に保存

```
[Step 1/3] TDnet新規開示を取得中...
  [OK] 完了
[Step 2/3] SQLite → Supabase push中...
  [OK] 完了
[Step 3/3] data.xlsx を生成中...
  [OK] 完了

  全ステップ完了
  出力: C:\Users\takuy\OneDrive\data.xlsx
```

> ログは `logs\` フォルダに自動保存されます（`update_20260301_091500.log` 等）。

---

## 1. 共有Excelで使う（TICKER_VIEW で ticker 入力→5年分が出る）

### 前提

- 共有Excelと `data.xlsx` が **同じフォルダ（OneDrive直下）** にあること
- 共有Excelの列: A列=銘柄コード, M列=決算期(例:R8/3), N列=四半期(例:2Q)

### 数式テンプレート（コピペしてください）

#### O列（累計売上）— 2行目に貼る場合

```
=IFERROR(INDEX([data.xlsx]DATA!$D$2:$D$10000,MATCH($A2&$M2&$N2,[data.xlsx]DATA!$A$2:$A$10000&[data.xlsx]DATA!$B$2:$B$10000&[data.xlsx]DATA!$C$2:$C$10000,0)),"")
```

#### P列（累計粗利）— 2行目に貼る場合

```
=IFERROR(INDEX([data.xlsx]DATA!$E$2:$E$10000,MATCH($A2&$M2&$N2,[data.xlsx]DATA!$A$2:$A$10000&[data.xlsx]DATA!$B$2:$B$10000&[data.xlsx]DATA!$C$2:$C$10000,0)),"")
```

#### S列（累計営業利益）— 2行目に貼る場合

```
=IFERROR(INDEX([data.xlsx]DATA!$F$2:$F$10000,MATCH($A2&$M2&$N2,[data.xlsx]DATA!$A$2:$A$10000&[data.xlsx]DATA!$B$2:$B$10000&[data.xlsx]DATA!$C$2:$C$10000,0)),"")
```

### 貼り方

1. 共有Excelを開く
2. O2セルを選択
3. 上の「O列」の数式をコピーして貼り付け
4. **Ctrl + Shift + Enter** で確定（※Microsoft 365の場合は普通にEnterでOK）
5. 数字が表示されたら成功！
6. O2セルを下方向にドラッグしてコピー
7. P列・S列も同じ手順で貼り付け

> **メモ列（Z列など）は数式と無関係なので、自由に手入力できます。壊れません。**

---

## 2. タスクスケジューラで自動実行する（10分ごと）

Windowsの「タスクスケジューラ」で自動実行できます。

### 設定手順

1. Windowsキーを押して **「タスクスケジューラ」** と入力して開く
2. 右側の **「基本タスクの作成」** をクリック
3. 名前: **TDnet全更新** → 次へ
4. トリガー: **毎日** → 次へ
5. 開始: **06:00:00** → 次へ
6. 操作: **プログラムの開始** → 次へ
7. 以下を入力:
   - プログラム: `run_update_all.bat` を「参照」で選ぶ
   - 開始（オプション）: `run_update_all.bat` があるフォルダのパス
8. **完了** をクリック

### 10分ごとに繰り返す設定

1. 作成したタスクをダブルクリック → 「トリガー」タブ
2. トリガーを選んで「編集」
3. **「繰り返し間隔」にチェック** → **10分** を選択
4. 「継続時間」→ **1日** に変更
5. OK → OK

> 18:00以降は開示がないため、「終了：18:00」にしておくと無駄な実行を防げます。

---

## 3. トラブル対処

| 画面の表示 | 原因 | やること |
|---|---|---|
| `Python環境が見つかりません` | .venvフォルダがない | 管理者に連絡 |
| `[ERROR] 失敗しました` | いずれかのステップでエラー | logs フォルダのログを確認 |
| `.env ファイルが見つかりません` | 設定ファイルがない | 管理者に連絡 |
| `インターネット接続を確認してください` | ネットが切れている | Wi-Fiを確認して再実行 |
| `data.xlsx を閉じてから再実行` | Excelで開いたまま実行した | data.xlsxを閉じて再実行 |
| `データベースへの接続が拒否されました` | 接続キーの期限切れ等 | 管理者に連絡 |
| `書き込み権限がありません` | OneDrive同期中 | 1分待って再実行 |

### ログの見方

ログファイルは `logs\` フォルダにあります:
- `update_20260301_091500.log`（ワンクリック全更新のログ）
- `data/app.log`（TDnet監視のログ）

> エラーが出たとき: ログファイルの最後の方を見てください。`[ERROR]` の行が原因です。

---

## 4. data.xlsx の列構成（参考）

| 列 | ヘッダー | 内容 |
|---|---|---|
| A | ticker | 銘柄コード (例: 0812) |
| B | period | 決算期 (例: R8/3) |
| C | quarter | 四半期 (例: 2Q) |
| D | net_sales | 累計売上（円） |
| E | gross_profit | 累計粗利（円）※短信未記載の場合は空欄 |
| F | operating_profit | 累計営業利益（円） |
| G | source_doc_id | 開示ID（自動） |
| H | disclosed_at | 開示日時（自動） |
| I | updated_at | 更新日時（自動） |

> **gross_profit（粗利）が空欄のことがあります** — 決算短信には粗利が記載されていないことが多いため正常です。

---

## 5. 動作確認チェックリスト

初めて使うとき、以下を1つずつ確認してください。

- [ ] ① `run_update_all.bat` をダブルクリック → 「全ステップ完了」と表示される
- [ ] ② `C:\Users\takuy\OneDrive\data.xlsx` が生成されている
- [ ] ③ data.xlsx を開くと DATA シートがあり、銘柄・数値が並んでいる
- [ ] ④ 共有Excelの O列に数式を貼ると数字が表示される
- [ ] ⑤ `logs\` フォルダにログファイルが作られている
